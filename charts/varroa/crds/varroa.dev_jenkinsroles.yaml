---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: jenkinsroles.varroa.dev
spec:
  group: varroa.dev
  names:
    kind: JenkinsRole
    listKind: JenkinsRoleList
    plural: jenkinsroles
    singular: jenkinsrole
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: JenkinsRole is a pure Jenkins permission set (data plane).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: JenkinsRoleSpec defines the desired state of JenkinsRole.
            properties:
              description:
                description: Description is a human-readable description of the role.
                type: string
              permissions:
                description: Permissions is the list of Jenkins permission IDs.
                items:
                  type: string
                type: array
              roleType:
                description: RoleType is "Global", "Item", or "Agent"; default "Global".
                type: string
            required:
            - permissions
            type: object
        type: object
    served: true
    storage: true
