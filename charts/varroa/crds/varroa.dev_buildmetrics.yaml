---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: buildmetrics.varroa.dev
spec:
  group: varroa.dev
  names:
    kind: BuildMetric
    listKind: BuildMetricList
    plural: buildmetrics
    singular: buildmetric
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.buildStatus
      name: BUILD STATUS
      type: string
    - jsonPath: .spec.durationSeconds
      name: DURATION
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BuildMetric is the Schema for the buildmetrics API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BuildMetricSpec defines the desired state of BuildMetric.
            properties:
              buildId:
                description: BuildId is the identifier of the build this metric belongs
                  to.
                type: string
              buildStatus:
                description: |-
                  BuildStatus is the outcome of the build.
                  Valid values: Success, Failure, Aborted, Running.
                type: string
              durationSeconds:
                description: DurationSeconds is the wall-clock duration of the build
                  in seconds.
                type: integer
              jobName:
                description: JobName is the Jenkins job name that produced this build.
                type: string
            required:
            - buildId
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
