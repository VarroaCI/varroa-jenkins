---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: users.varroa.dev
spec:
  group: varroa.dev
  names:
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User.
            properties:
              displayName:
                description: DisplayName is a human-readable display name.
                type: string
              email:
                description: Email is the email address of the user.
                type: string
              password:
                description: |-
                  Password is a write-only field set by admins. The UserReconciler
                  hashes this value into status.credentials.passwordHash and clears it.
                type: string
            type: object
          status:
            description: UserStatus defines the observed state of User.
            properties:
              activeControllers:
                description: ActiveControllers lists controller names this user has
                  active sessions on.
                items:
                  type: string
                type: array
              credentials:
                description: |-
                  Credentials holds the hashed password for local auth users.
                  Only set when auth mode is "local".
                properties:
                  lastChanged:
                    format: date-time
                    type: string
                  passwordHash:
                    type: string
                type: object
              lastLogin:
                description: LastLogin is the timestamp of the user's most recent
                  login.
                format: date-time
                type: string
              observedGroups:
                description: |-
                  ObservedGroups captures the groups from OIDC claims at login time,
                  enabling read-only group-by views in OIDC mode.
                items:
                  type: string
                type: array
              preferences:
                description: Preferences stores user-customizable UI settings.
                properties:
                  accent:
                    type: string
                  defaultLanding:
                    type: string
                  theme:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
