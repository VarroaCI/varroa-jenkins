---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: varroaroles.varroa.dev
spec:
  group: varroa.dev
  names:
    kind: VarroaRole
    listKind: VarroaRoleList
    plural: varroaroles
    singular: varroarole
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.varroa\.dev/builtin
      name: BUILTIN
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VarroaRole is the Schema for the varroaroles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VarroaRoleSpec defines the desired state of VarroaRole.
            properties:
              apiRules:
                description: APIRules defines the BFF API authorization rules for
                  this role.
                items:
                  description: APIRule defines a single API authorization rule with
                    resources and verbs.
                  properties:
                    resources:
                      items:
                        type: string
                      type: array
                    verbs:
                      items:
                        type: string
                      type: array
                  required:
                  - resources
                  - verbs
                  type: object
                type: array
              jenkinsPermissions:
                description: |-
                  JenkinsPermissions defines the Jenkins data-plane permissions for this role.
                  Deprecated: use JenkinsRoleRef to reference a JenkinsRole CR instead.
                items:
                  type: string
                type: array
              jenkinsRoleRef:
                description: |-
                  JenkinsRoleRef links this API role to a data-plane JenkinsRole.
                  The referenced JenkinsRole MUST be RoleType=Global.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
